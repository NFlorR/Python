const input = document.querySelector('input');
const boton = document.querySelector('btn');
const listas = document.querySelector('lists');

boton.addEventListener("click", function() {
    input.value=""
    listas.appendChild(listItem)
});
